from assignments_ACT.functions import speed_calucation
u = int(input("The initial velocity of the object: "))
t = int(input("The time it takes for the object to move: "))
a = int(input((" The acceleration of the object: ")))

speed_calucation(u,t,a)